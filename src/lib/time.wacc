begin
  # Returns EPOCH time in seconds
  int time()

  # Sleeps for the specific number of seconds
  int sleep(int seconds)

  # Returns the current year
  int currentYear()

  # Returns the current month of the year as an int (starting from 1)
  int currentMonth()
end