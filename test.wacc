# basic array (reference) printing

# Output:
# Printing an array variable gives an address, such as #addrs#
#

# Program:

begin
  int addTwo(int x, int y) is
    return x + y
  end
  z = 1;
  x = 2;
  y = 3;
  a = call addTwo(x, y);
  println a
end
