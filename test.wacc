# basic array (reference) printing

# Output:
# Printing an array variable gives an address, such as #addrs#
#

# Program:

begin
  addTwo(int x, int y) is
  return x + y
  end
  x = 2;
  y = 3;
  p = newpair(1, 1);
  a = call addTwo(x, y);
  fst p = 69;
  snd p = fst p;
  p = p;
  p = newpair(1,2);
  print a
end